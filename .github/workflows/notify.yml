name: Check IT Passport

on:
  schedule:
    - cron: '0,30 * * * *'  # 30分ごとの実行（UTC基準）
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: pip install pdfplumber requests

    - name: Run script
      run: python main.py
